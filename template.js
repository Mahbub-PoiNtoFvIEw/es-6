/**
 * backtick(``), powerful and important symbol in JS
 * used for string interpolation
 * used for multi line string
 */

const a = 10;
const b = 20;
const result  = `The sum of ${a} and ${b} is ${a+b}`;//string interpolation
console.log(result);

const x = `I want to complete this corse,
no matter what situation i need to face,
I will continue this course till end`;// multi line string
console.log(x);